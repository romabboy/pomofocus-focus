import React from "react";
import { DropDownProps } from "./types";
 

export const DropDownColor: React.FC<DropDownProps> = ({isOpen}) => {
    return (
        <ul>
            
        </ul>
    )
}