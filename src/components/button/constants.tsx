import { ButtonVariants } from "./types";

export const BUTTON_STYLE_VARIANTS = {
    [ButtonVariants.NAV_HEADER]:
    "text-xs py-2 px-3 rounded-sm bg-white hover:bg-opacity-35 bg-opacity-25 transition-colors duration-100 flex items-center font-medium gap-1"
}